services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rawdb-dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
    entrypoint: /bin/bash
